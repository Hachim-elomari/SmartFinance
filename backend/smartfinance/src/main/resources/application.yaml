spring:
  application:
    name: smartfinance

  datasource:
    url: jdbc:postgresql://localhost:5432/smartfinance
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080